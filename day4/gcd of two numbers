#include<stdio.h>
#include<stdlib.h>
int main()
{
int a,b,i,gcd;

printf("enter the a and b values\n");
scanf("%d %d",&a,&b);
for(i=1;i<=a && i<=b;i++)
{ 
if(a%i==0 && b%i==0)
 gcd=i;
}
printf("gcd of two numbers is%d\n",gcd);
return 0;
}
