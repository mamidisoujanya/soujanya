#include<stdio.h>
#include<stdlib.h>
int main()
{
int a,b,i,lcm,gcd;

printf("enter the a and b values\n");
scanf("%d %d",&a,&b);
printf("lcm is\n");

for(i=1;i<=a && i<=b;i++)
{ 
if(a%i==0 && b%i==0)
 gcd=i;
}
lcm=a*b/gcd;
printf("%d",lcm);
return 0;
}
