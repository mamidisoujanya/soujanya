#include<stdio.h>
#include<stdlib.h>
int main()
{
int i,n,fact=1;
printf("enter the n value which num you want find factorial\n");
scanf("%d",&n);

  for(i=1;i<=n;i++)
  {
  fact=fact*i;
  }

printf("factorial of num is %d\n ",fact);
return 0;
}
