#include<stdio.h>
#include<stdlib.h>
int main()
{
char str[100]="company";
char str1[100]; 
int i,j,count=0;
while(str[count]!='\0')

count++;
j=count-1;
for(i=0;i<count;i++)
{
str1[i]=str[j];
j--;
}
printf("%s\n",str1);
return 0;
}
